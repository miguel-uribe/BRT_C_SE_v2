# Primero:
 - Usar datos de la mañana para rutas al norte
 - Simular todas las configuraciones posibles en la franja AM, usando sólo las rutas hacia el norte.
 - Sacar un pareto usando retraso vs. similitud de rutas obtener una configuración óptima.

# Segundo:
 - Usar datos de la tarde para todas las rutas
 - Usar la configuración óptima hacia el norte. 
 - Y simular todas las posibles configuraciones hacia el sur.
 - Evaluar 3 objetivos: retraso hacia el sur - similitud de rutas - saturación máxima en los vagones (max_{sobre_los_vagones}(sum_{sobre las rutas en cada vagón} dwell_time/headway)). 

# Tercero:
 - Usar los datos de la mañana
 - Usar la configuración óptima hacia el sur
 - Probar todos los vagones para la ruta que sólo pasa en la mañana y evaluar retraso vs similitud

